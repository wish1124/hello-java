package lesson1;

/*public class Operator {

    // 정수형 숫자를 더하는 메서드
    public int add(int a, int b) {
        return a + b;
    }

    // 실수형 숫자를 더하는 메서드
    public double add(double a, double b) {
        return a + b;
    }

    // 정수형 세 개의 숫자를 더하는 메서드 (오버로딩 예시)
    public int add(int a, int b, int c) {
        return a + b + c;
    }

    // 실수형 세 개의 숫자를 더하는 메서드 (오버로딩 예시)
    public double add(double a, double b, double c) {
        return a + b + c;
    }

    public static void main(String[] args) {
        Operator operator = new Operator();

        // 정수형 더하기
        System.out.println("정수형 더하기: " + operator.add(5, 10)); // 출력: 15

        // 실수형 더하기
        System.out.println("실수형 더하기: " + operator.add(5.5, 10.2)); // 출력: 15.7

        // 정수형 세 개의 숫자를 더하기
        System.out.println("세 개의 정수형 더하기: " + operator.add(5, 10, 15)); // 출력: 30

        // 실수형 세 개의 숫자를 더하기
        System.out.println("세 개의 실수형 더하기: " + operator.add(1.1, 2.2, 3.3)); // 출력: 6.6
    }
}*/

/*public class VariableArgsPrinter {

    // 가변 인수를 받는 메서드
    public void printStrings(String... strings) {
        for (String str : strings) {
            System.out.println(str);
        }
    }

    public static void main(String[] args) {
        VariableArgsPrinter printer = new VariableArgsPrinter();

        // 문자열 인수를 여러 개 넘겨서 출력
        printer.printStrings("Hello", "World", "Java", "Programming");

        // 문자열 인수를 하나만 넘겨서 출력
        printer.printStrings("Single String");

        // 문자열 인수를 넘기지 않은 경우 (빈 메서드 호출)
        printer.printStrings();
    }
}*/
/*Method Name: sum
Return Type: int
Parameters:
    - int a: First integer parameter
    - int b: Second integer parameter*/
